<!DOCTYPE html>
<html>
  <head>
    <title>ACME Pizza</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render partial: 'header' %>

    <div>
      <% flash.each do |key, value| %>
        <div class="center">
          <%= value %>
        </div>
      <% end %>
    </div>

    <div>
      <%= yield %>
    </div>
  </body>
  
</html>
